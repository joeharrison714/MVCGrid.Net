@using MVCGrid.Web
@{
    ViewBag.Title = "Vertical Engine";
}

@section head {
    
}

<h2>@ViewBag.Title</h2>

<p>Resize your browser window, shrinking it until the grid flips to a vertical display of the table.  To do this, you need to surround your grid with a div with a class that is defined in the CSS below.</p>

<div class="vertical-table">
    @Html.MVCGrid("VerticalEngineGrid")
</div>

<h3>Code</h3>

<div class="panel panel-default">
    <div class="panel-heading">Inside <strong>MVCGridConfig.cs</strong></div>
    <div class="panel-body">
        <pre class="brush: csharp">@CodeSnippetHelper.GetCodeSnippet("FormattingGrid")</pre>
    </div>
</div>

<div class="panel panel-default">
    <div class="panel-heading">View</div>
    <div class="panel-body">
        <pre>@@Html.MVCGrid("VerticalEngineGrid")</pre>
    </div>
</div>

<div class="panel panel-default">
    <div class="panel-heading">css</div>
    <div class="panel-body">
        <pre>
@@media only screen and (max-width: 768px) {
/* Force table to not be like tables anymore */
.vertical-table table,
.vertical-table thead,
.vertical-table tbody,
.vertical-table th,
.vertical-table td,
.vertical-table tr {
    display: block;
}
/* Hide table headers (but not display: none;, for accessibility) */
.vertical-table thead tr {
    position: absolute;
    top: -9999px;
    left: -9999px;
}
.vertical-table tr {
    border: 1px solid #ccc;
}
.verticalCellLabel {
    /* Behave  like a "row" */
    width: 50% !important;
}
.verticalCellLabel {
    display: inline-block !important;
    font-weight: bold;
}
}
        </pre>
    </div>
</div>
